import { FormatCardCodePipe } from './format-card-code.pipe';

describe('FormatCardCodePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCardCodePipe();
    expect(pipe).toBeTruthy();
  });
});
